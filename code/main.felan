@import("basic.felan");

f :: (a:[]u8)->void{
  i := 0u64;

  while i < a.length {
    c := a[i];
    if c == '{' {

    }
    print_char(a[i]);
    i += 1u64;
  }
};

main :: () -> void {
  f("Hello {a} How {b}");
};

